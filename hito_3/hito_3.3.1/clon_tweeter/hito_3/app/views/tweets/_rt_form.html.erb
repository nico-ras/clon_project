<%= form_with(model: tweet) do |form| %>
  <% if tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
        <% tweet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  Post your tweet! :

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>

  <div class="field">
    <%= form.hidden_field :retweeted_id, value: @retweet.id %>
  </div>

    <div>
      <%= "Retweeting from : #{ @retweet.user.email}" %>

      <div>
          <%= "#{@retweet.content}" %>
      </div> 
    </div>




  

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>